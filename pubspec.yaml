name: hydracoach
description: "Smart hydration tracking with electrolyte balance"
publish_to: 'none'

# Версия приложения: 2.0.0 (major notification system overhaul) + номер сборки 1
version: 2.0.0+15 # версия+номер_сборки

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  
  # 🌍 ЛОКАЛИЗАЦИЯ
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # UI и иконки
  cupertino_icons: ^1.0.8
  
  # Стейт менеджмент
  provider: ^6.1.5+1
  
  # Локальное хранение
  shared_preferences: ^2.5.3
  
  # Утилиты
  http: ^1.5.0
  timezone: ^0.10.0
  # flutter_timezone удален - используем ручное определение таймзоны
  
  # UI компоненты
  fl_chart: ^1.0.0
  flutter_animate: ^4.5.2
  carousel_slider: ^5.0.0
  
  # Разрешения и уведомления
  flutter_local_notifications: ^19.4.1
  geolocator: ^14.0.2
  permission_handler: ^11.0.1
  
  # 🔥 FIREBASE - основные сервисы
  firebase_messaging: ^15.0.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.0
  firebase_analytics: ^11.3.0
  firebase_remote_config: ^5.1.0
  firebase_crashlytics: ^4.1.0
  
  # 🔐 GDPR/CONSENT MANAGEMENT
  usercentrics_sdk: ^2.23.0
  
  # 🚀 APPSFLYER - основная аналитика, атрибуция и ROI360
  appsflyer_sdk: ^6.13.3

  # 📊 Аналитика DevToDev
  # Используем локальный DevToDevAnalyticsService через method channel без pub-зависимости

  # 📁 ФАЙЛЫ - экспорт данных
  path_provider: ^2.1.5
  csv: ^6.0.0
  simple_animations: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # 🎨 Для генерации иконок
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  
  # 🌍 ГЕНЕРАЦИЯ ЛОКАЛИЗАЦИИ
  generate: true
  
  # 🎨 Assets и иконки
  assets:
    - assets/images/
    - assets/notifications/
  
  # Шрифты (если будем добавлять кастомные)
  # fonts:
  #   - family: Montserrat
  #     fonts:
  #       - asset: fonts/Montserrat-Regular.ttf
  #       - asset: fonts/Montserrat-Bold.ttf
  #         weight: 700

# 🎨 КОНФИГУРАЦИЯ ИКОНОК
flutter_launcher_icons:
  android: "ic_launcher"  # Важно: используем ic_launcher, не launcher_icon
  ios: true
  image_path: "assets/images/app_icon.png"
  min_sdk_android: 21
  
  # Адаптивная иконка для Android
  adaptive_icon_background: "#00D4DB"  # Бирюзовый фон
  adaptive_icon_foreground: "assets/images/app_icon.png"  # Пока та же капля, нужно будет заменить на белую
  
  # iOS настройки
  remove_alpha_ios: true
